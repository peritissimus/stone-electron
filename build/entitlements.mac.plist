<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow app to be run on macOS -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- File access for notes storage -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Network access (for future sync features) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Microphone access — required by the meeting recorder. macOS
         prompts on first record regardless; this entitlement is needed
         for packaged builds running under hardened runtime. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
